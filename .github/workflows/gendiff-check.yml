name: gendiff-check

on:
  - push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run linter
        run:
          make lint
      - name: Run tests
        run:
          make tests
